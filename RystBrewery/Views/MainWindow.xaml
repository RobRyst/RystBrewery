<Window x:Class="RystBrewery.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
       mc:Ignorable="d"
       Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top controls: Where to select program + Start the program -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Velg program:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Width="150" ItemsSource="{Binding ProgramOptions}" SelectedItem="{Binding SelectedProgram, Mode=TwoWay}"/>
            <Button Content="Start" Margin="10,0,0,0" Click="Start_Click"/>
        </StackPanel>

        <!-- Chart displaying temperature -->
        <lvc:CartesianChart Grid.Row="1" Series="{Binding TempSeries}" LegendPosition="Right" />
    </Grid>
</Window>
