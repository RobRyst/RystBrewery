<Window x:Class="RystBrewery.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
       mc:Ignorable="d"
       Title="MainWindow" Height="600" Width="1200">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#EEE" Margin="10">
            <TextBlock Text="Home" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />
            <Button Content="Dashboard" Click="Dashboard_Click" Margin="1"/>
            <Button Content="Ryst IPA" Click="TankOneBtn_Click" Margin="1"/>
            <Button Content="Ryst Eplecider" Click="TankTwoBtn_Click" Margin="1"/>
            <Button Content="Ryst Sommerøl" Click="TankThreeBtn_Click" Margin="1"/>
        </StackPanel>

        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Frame x:Name="MainContentFrame" Grid.Column="1" Grid.RowSpan="2" Margin="10" NavigationUIVisibility="hidden" />
        </Grid>
    </Grid>
</Window>
